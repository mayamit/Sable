# Personal Semantic Layer (PSL/i) — marketing website

Marketing site for **Personal Semantic Layer (PSL/i)** (the product), by **PSL**.

## Start here
1. `CLAUDE.md` (repo root) — the rules Claude Code must follow every session.
2. `docs/Sable_Website_Design_Brief.md` — full design + build brief (stack, sitemap, homepage anatomy, tokens, prompt sequence).
3. `docs/PSL_Proposed_Sitemap.svg` — information architecture.

## Reference
- `docs/reference/mockups/` — first-pass V1 surface mockups (Communications lens, Reasoning lens, Navigator approval). Also serve as a component/style reference.
- `docs/reference/design/` — visual-language references. **`UI_Work_Surface_V3_VISION.jpeg` is the V3 vision — do not present it as the V1 product.**
- `docs/research/` — background competitive research (not needed to build).

## Building
The site itself (Astro + Tailwind + MDX) is not scaffolded yet. Follow the prompt sequence in the brief (§12): scaffold → tokens + layout shell → Hero + How-it-works → components → remaining pages.

## Note
Content in `docs/` includes internal positioning and strategy. If this repo is **public**, move `docs/` to a private repo.
